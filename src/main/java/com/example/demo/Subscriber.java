package com.example.demo;

import ch.qos.logback.core.model.Model;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;

import java.util.List;

public class Subscriber {
 /*   private String name;

    @GetMapping("/projet/{projectName}")
    public String afficherProjet(@PathVariable String projectName, Model model) {
        // Récupérer les informations du projet et les commentaires associés
        String project = CommentaireService.getCommentairesByProject(projectName).toString();
        List<commentaire> commentaires = CommentaireService.getCommentairesByProject(projectName);  // Service pour récupérer les commentaires

        //model.addAttribute("projet", project);
        //model.addAttribute("commentaires", commentaires);  // Passer les commentaires au modèle

        return "dashboarduser";  // Page Thymeleaf du projet
    }
*/
}
